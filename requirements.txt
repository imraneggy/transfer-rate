httpx==0.27.2
beautifulsoup4==4.12.3
lxml==5.3.0
# Optional: headless-Chromium fallback for providers that JS-render
# their rates (currently only LuLu). Activated by setting
# LULU_USE_BROWSER=1; the .github/workflows/scrape.yml caches the
# Chromium binary so installs only run on a cache miss.
playwright==1.50.0
