---
name: Machine handover 2026-05-15 (v0.32.5 baseline)
description: Workstation handed to Ali & Sons IT after v0.32.5 shipped. Full Claude context backed up to imraneggy/transfer-rate in .claude-context/. New-machine continuation guide is in that directory.
type: project
originSessionId: 76d9bf25-7e54-4c08-a667-1570a1f76389
---
**Event:** Personal Windows workstation handed over to Ali & Sons IT
on 2026-05-15.  Backup committed in commit `6bf806e` on main of
`github.com/imraneggy/transfer-rate` under `.claude-context/`.

**Project state at handover:**
- Latest released app version: **v0.32.5**
- Last shipped fix: `_merge_uae_gold_history` pipeline bug — was
  truncating igold.ae's 30-day gold history to 2 cron-snapshot
  entries.  After fix: full 30 days end-to-end for both gold and
  silver, both UAE and India.
- iOS port: discussed but paused pending Mac access + Apple Developer
  Program.  Three paths weighed (SwiftUI / KMP+Compose-MP / PWA);
  SwiftUI recommended.

**How a future Claude session can resume:**
1. The `.claude-context/CONTINUATION_GUIDE.md` in the repo has the
   complete handover walkthrough.
2. Memory files including this one were copied to
   `.claude-context/memory/` before the move — so this exact context
   is in git, retrievable on any machine via `cp -r .claude-context/
   memory/* ~/.claude/projects/*transfer-rate*/memory/`.

**Why:** If user references "the handover" or asks to "continue from
where we left off", check this memory entry + the
CONTINUATION_GUIDE.md in the repo to understand context.

**How to apply:** On any new-machine start of this project, after the
memory restore, the resumption point is "v0.32.5 is shipped and stable,
all four metal columns have 30-day history, iOS port awaiting Mac
access, feature roadmap untouched since the v0.32 burst".
